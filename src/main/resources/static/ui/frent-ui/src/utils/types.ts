export type Movie = {
    title: string, 
    price: number,
    isAvailable: boolean,
    description: string, 
    dueDate: string,
    isRented: boolean
}

export type Notification = {
    message: string
}